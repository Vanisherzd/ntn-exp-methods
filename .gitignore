# Virtual environments & dependency caches
.venv/
.deps/
.eggs/
*.egg-info/

# Python bytecode
__pycache__/
*.py[cod]
*.so

# Test / coverage artifacts
.pytest_cache/
.coverage
htmlcov/

# Data and model weights
outputs/
# Regenerable build artifacts & scratch (slides PDF render, temp PNG tiles)
output/
tmp/
data/tle/*.npz
data/tle/*.txt
!data/tle/.gitkeep
models/*.pt
models/*.pth
*.ckpt

# Notebooks
notebooks/
*.ipynb

# OS artifacts
.DS_Store
Thumbs.db
*.swp
*.swo

# UV lockfile (pinned env, not needed in repo)
uv.lock

# Large dataarchives
# Large binary assets (> GitHub 100 MB hard limit)
# Store in Git LFS: git lfs track "*.npy" "*.h5" "*.pb"
payload_results_realizations/hwil_usrp/*.npy
payload_results_realizations/hwil_usrp/*.json
.hermes.md
payload_results_realizations/live_usrp/
# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.log
*.out
*.nav
*.snm
*.toc
paper/*.pdf

# Packet-validation run outputs (regenerable; do not commit)
validation_runs/

# Raw IQ captures (large; keep local)
*.fc32
*.cfile
*.sigmf-data
hardware_conducted_iq/**/*.npy
hardware_conducted_iq/**/*.fc32
hardware_conducted_iq/**/*.cfile

# OTA IQ replay raw captures (large; keep local). Curated CSV/JSON/figures stay tracked.
hardware/ota_iq/runs/**/*.npy
hardware/ota_iq/runs/**/capture_iq.*
hardware/ota_iq/inputs/

# Hardware local artifacts (raw IQ + compiled tools never committed)
hardware/usrp_scripts/rx_capture_to_file_cpp
hardware/captures/*.fc32
hardware/captures/*.dat
hardware/captures/*.png
hardware/captures/*.json
hardware/captures/**/*.fc32
hardware/captures/**/*.dat
hardware/captures/**/*.png
hardware/captures/**/*.json
hardware/captures/**/*.csv
hardware/captures/**/*.log

# Curated evidence + paper figures ARE committed (override the broad ignores above,
# incl. the global *.log rule). Raw .fc32/.dat are still never tracked here.
!hardware/artifacts/
!hardware/artifacts/**/*.png
!hardware/artifacts/**/*.json
!hardware/artifacts/**/*.csv
!hardware/artifacts/**/*.log
!hardware/artifacts/**/*.md
!paper/figures/hardware/
!paper/figures/hardware/**/*.png
!paper/figures/hardware/**/*.md

# Local raw-IQ archive (never committed)
hardware/local_raw_archive/

semtech_validation/lr1121_tx_config_example.json

# Decoded-RX example fixtures are committed (not build logs)

# Local-only archive of raw evidence / generated runs / build artifacts
local_archive/

# Data registry: raw + processed datasets are local-only (only schemas,
# manifests, and tiny examples under data/ are committed).
data_raw/
data_processed/

# Local advisor review package
advisor_package/

# local secrets
.env
.env.*
*.env
*.secret
*.secrets

# raw/local-only orbital data
dataraw/
# Local secrets
.env
.env.*
*.env
.env.spacetrack

# Flash dumps (raw board images; never commit)
hardware_conducted_iq/board_inventory/**/board_*_flash_dump*.bin
hardware_conducted_iq/board_inventory/**/*.bin
# Firmware build artifacts
build/
firmware_patches/**/build/
