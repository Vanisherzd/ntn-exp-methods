\documentclass[conference]{IEEEtran}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{cite}
\usepackage{booktabs}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{etoolbox}
\usepackage{enumitem}
\setlist[itemize]{leftmargin=1.3em,itemsep=2.5pt,topsep=3pt,parsep=0pt}

% Moderate vertical spacing (readable, not over-compressed)
\setlength{\textfloatsep}{7pt plus 2pt minus 2pt}
\setlength{\dbltextfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\floatsep}{7pt plus 2pt minus 2pt}
\setlength{\intextsep}{7pt plus 2pt minus 2pt}
\setlength{\abovedisplayskip}{4pt plus 1pt minus 1pt}
\setlength{\belowdisplayskip}{4pt plus 1pt minus 1pt}
\setlength{\abovedisplayshortskip}{3pt}
\setlength{\belowdisplayshortskip}{3pt}
% Mild bibliography spacing (font unchanged, readable)
\let\WLDthebibliography\thebibliography
\renewcommand\thebibliography[1]{\WLDthebibliography{#1}%
  \setlength{\itemsep}{1pt}\setlength{\parsep}{0pt}}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,fit,backgrounds,calc}

% Horizontal justification for table notes
\patchcmd{\table}{\refstepcounter{table}}{\refstepcounter{table}}{}{}

% Pack floats tighter (workshop page budget)
\renewcommand{\topfraction}{0.95}
\renewcommand{\dbltopfraction}{0.95}
\renewcommand{\textfraction}{0.05}
\renewcommand{\floatpagefraction}{0.85}
\renewcommand{\dblfloatpagefraction}{0.85}
\setcounter{topnumber}{3}
\setcounter{dbltopnumber}{3}
\setcounter{totalnumber}{4}

\hyphenation{LEO-PGRL-LRFHSS}

\begin{document}

% ── Title ────────────────────────────────────────────────────────────────────
\title{Evidence-Gated Timing/Frequency Control for LR-FHSS Direct-to-Satellite IoT}

\author{
  \IEEEauthorblockN{Anonymous Authors}
  \IEEEauthorblockA{Affiliations omitted for double-blind review}
}

\maketitle

% ── Abstract ─────────────────────────────────────────────────────────────────
\begin{abstract}
Direct-to-Satellite (D2S) IoT terminals must pre-compensate large LEO Doppler
shifts before every LR-FHSS uplink from an on-board orbital prediction that is, in
practice, a \emph{stale} TLE propagated open-loop by SGP4. We recast this classical
PHY-layer Doppler-compensation problem as timing- and frequency-uncertainty-aware
\emph{endpoint control} under stale orbital information and low-power constraints:
prediction uncertainty sizes the transmit-window guard, the hop-bin margin, and the
energy per successful burst. A natural idea is to learn a residual correction on
top of the physics baseline. On real Space-Track TLE history for two BLACK~KITE
LEO satellites (NORAD~66741, 68474), with strict chronological leakage-free splits,
a learned Doppler residual does \emph{not} beat the open-loop baseline at any
tested staleness (8--168~h), target-specific or cross-satellite. Rather than
discard learning, we gate it: the \emph{Evidence Gate} enables a learned corrector
only when a chronological held-out window beats the baseline by a margin~$\gamma$,
else falls back to open-loop compensation. On the real data the gate \emph{closes};
in a controlled synthetic stress study it \emph{opens} under dominant systematic
drift---the gate's value is preventing unsupported learning, opening only under
held-out evidence. Software-only proxies show joint timing--frequency control moves proxy
success from ${<}1\%$ to $\sim$90\% only in that controlled synthetic gate-open
regime, at an inference-only MCU footprint estimate, while a conducted IQ-level
run verifies only the cabled measurement path (TX-ON $41.25\pm0.36$~dB above
TX-OFF over 50~dB attenuated coax). All ``reference'' Doppler is model-derived
(SGP4 from real TLE), not measured; no packet, link-layer, over-the-air, or
live-satellite result is claimed.
\end{abstract}

\begin{IEEEkeywords}
Direct-to-Satellite IoT, LR-FHSS, endpoint control, timing uncertainty, stale TLE,
SGP4, Doppler residual, conservative learning gate, LEO satellite
\end{IEEEkeywords}

% ── Introduction ─────────────────────────────────────────────────────────────
\section{Introduction}
\label{sec:intro}

Direct-to-Satellite (D2S) IoT using Low-Earth Orbit (LEO) satellites enables
connectivity for power-constrained end-devices in remote areas, but the high
relative velocity (several~km/s in LEO) induces Doppler shifts of tens of
kHz---order $20$~kHz at 868~MHz and roughly $50$~kHz at S-band, depending on
carrier frequency and pass geometry---together with rapid Doppler-rate changes
that stress the physical layer~\cite{hurn2023lrfhss}. Long-Range
Frequency-Hopping Spread Spectrum (LR-FHSS), characterized in Semtech's
AN1200.64 note~\cite{semtech2023lrfhss}, achieves robust uplink by distributing
transmit energy across a grid of narrow frequency bins, providing frequency
diversity against Doppler uncertainty. Because the sub-channels are narrow,
residual frequency error after pre-compensation is costly: it widens the guard
the terminal must reserve, increases the chance of missing a hop bin, and spends
airtime and energy that a battery-powered, unattended node cannot recover.

The terminal must size these margins \emph{before} transmitting, using only the
orbital information it holds on board---in practice a \emph{stale} TLE propagated
open-loop by SGP4/SDP4~\cite{vallado2007fundamentals}. This is the deployable
physics baseline. We transform classical PHY-layer Doppler compensation into
timing- and frequency-uncertainty-aware endpoint control for LR-FHSS D2S IoT under
stale orbital information and low-power constraints: prediction uncertainty is not
merely an accuracy statistic but the quantity that sizes the transmit-window guard,
the hop-bin frequency margin, and the energy spent per successful burst. A tempting
hypothesis is that a learned model can predict the
residual between a stale-TLE prediction and a later-TLE model-derived reference,
shrinking the error. The central
question of this paper is therefore not how to recover a waveform once it
exists, but a transmitter-side decision: \emph{when is it justified to learn a
correction on top of the physics prediction, and when is the safe physics
default the right choice?}

We expose a gap in the naive ``always learn'' answer: on real TLE history for two
BLACK~KITE satellites, a learned residual corrector does not beat the open-loop
SGP4 / stale-TLE baseline at any tested staleness---an always-on learner would, if
anything, \emph{increase} the frequency error it is meant to reduce. Learning
must therefore be \emph{earned} on held-out evidence, formalised as the
\emph{Evidence Gate}: a learned corrector deploys only when a chronological
held-out validation window shows it beats the physics baseline by a margin.

\emph{Related work.} Prior art clusters into four layers, none of which contains
our decision problem. (i)~\emph{Link/capacity/energy} analyses quantify aggregate
LR-FHSS reliability and transmission cost under LEO Doppler and
load~\cite{semtech2023lrfhss,boquet2021lrfhss,ullah2022lrfhss,hurn2023lrfhss}.
(ii)~\emph{Receiver-side} work---transceiver design~\cite{jung2023lrfhss} and
real-world packet-trace studies~\cite{bukhari2023lrfhss}---recovers packets
\emph{after} a transmission exists. (iii)~\emph{NTN synchronisation} estimates
timing advance and ToA/CFO, largely at the receiver or random-access
layer~\cite{3gpp2019tr38821}. (iv)~\emph{Orbit-learning} corrects
SGP4/SDP4~\cite{hoots1980models} trajectories---GP
correction~\cite{peng2019gp}, differentiable SGP4~\cite{acciarini2025dsgp4},
survey~\cite{caldas2024mlorbit}---targeting trajectory accuracy, while D2S
uplink policies~\cite{alvarez2022uplink} choose \emph{when} to transmit given a
prediction assumed trustworthy. None decides, at the endpoint and \emph{before}
transmission, whether a learned residual should be trusted under stale orbital
information, nor how timing/frequency uncertainty should size the LR-FHSS
guard, hop-bin margin, and energy policy.

Unlike receiver-side LR-FHSS studies and orbit-accuracy learning, we study the
\emph{pre-transmission endpoint decision}: is a learned Doppler residual
trustworthy enough to size timing/frequency/energy margins, or should the
terminal retain the SGP4 physics default? Contributions:
\begin{itemize}
  \item \emph{Evidence-gated endpoint control under stale orbital information:}
  a deploy/no-deploy trust test (Eqs.~\eqref{eq:gate}--\eqref{eq:deploy}) at the
  Doppler-precompensation point with causal chronological windows; no
  distribution-shift guarantee is claimed.
  \item \emph{Real-data negative control plus controlled gate-open condition:}
  on BLACK~KITE TLE history (8--168~h, strict splits) the learned residual never
  beats SGP4, so the gate closes; a synthetic systematic regime opens the same
  rule (Tables~\ref{tab:bk}--\ref{tab:gate}, Fig.~\ref{fig:gateev}); no
  real-data improvement from learning is claimed.
  \item \emph{Endpoint-budget proxies plus bounded conducted-IQ evidence:} the
  coverage-proxy chain (Eqs.~\eqref{eq:guard}--\eqref{eq:esucc}) links
  timing/frequency uncertainty to guard, hop-bin margin, and energy per
  successful burst at an MCU-class footprint (Fig.~\ref{fig:proxies}), plus a
  repeatable conducted measurement-path sanity check; no packet, link-layer, or
  OTA validation is claimed.
\end{itemize}

% ── System Model ──────────────────────────────────────────────────────────────
\section{System Model}
\label{sec:system}

\subsection{LEO D2S Channel Characteristics}
In a LEO pass the one-way Doppler reaches the tens-of-kHz magnitudes of
Sec.~\ref{sec:intro}, with the largest rate near closest approach. The Doppler
shift is modeled as
\begin{equation}
f_D(t) = -\frac{v_r(t)}{c}\,f_c
\label{eq:doppler}
\end{equation}
where $v_r(t)$ is the radial velocity (positive when satellite recedes) and $c$
is the speed of light. The residual CFO after pre-compensation is
\begin{equation}
f_{\mathrm{res}}(t) = f_D(t) - \hat{f}_D(t) + f_{\mathrm{osc}},
\label{eq:residual_cfo}
\end{equation}
where $\hat{f}_D$ is the predicted Doppler and $f_{\mathrm{osc}}$ is the terminal
oscillator offset. We model reliable hop reception as $|f_{\mathrm{res}}|$ staying
small relative to a fixed frequency tolerance $F_{\mathrm{tol}}$; this is a control
proxy, not a standard-compliant receiver model.

\subsection{Stale-TLE Open-Loop Baseline and the Learned Residual}
\label{sec:baseline}
The terminal holds a TLE that is, in general, older than the present epoch.
Propagating this \emph{stale} TLE with SGP4~\cite{hoots1980models} gives the
deployable open-loop Doppler prediction $f_{\mathrm{phys}}(t)$. To study whether
this can be improved, we define a model-derived \emph{reference} Doppler
$D_{\mathrm{ref}}(t)$ as the SGP4 propagation of a \emph{later} TLE for the same
satellite at the same UTC. We emphasise that
\texttt{reference\_is\_measured\_truth = false}: $D_{\mathrm{ref}}$ is a newer
TLE solution, not an RF measurement of satellite Doppler. The quantity a learner
would have to predict is the inter-TLE residual
\begin{equation}
r(t) = D_{\mathrm{ref}}(t) - f_{\mathrm{phys}}(t),
\label{eq:residual}
\end{equation}
which encodes mean-element updates, drag-term changes, and numerical fit
differences between successive TLE solutions. A learned corrector forms
$f_{\mathrm{ml}}(t)=f_{\mathrm{phys}}(t)+\hat r(x_t)$ from a feature vector $x_t$
(TLE age, epoch gap, stale Doppler, orbital phase, geometry, and stale orbital
elements). All Doppler is computed at 868~MHz for a representative ground station
(24$^\circ$N, 121$^\circ$E).

% ── Method: Evidence Gate ─────────────────────────────────────────────────────
\section{Evidence-Gated Endpoint Control}
\label{sec:method}

\begin{figure*}[!t]
    \centering
    \resizebox{0.93\textwidth}{!}{%
    \begin{tikzpicture}[
        font=\scriptsize, >=Latex,
        blk/.style={draw=black!70, line width=0.5pt, align=center,
                    minimum height=7.5mm, inner sep=3pt, fill=white},
        sig/.style={->, line width=0.7pt, black!75},
        phys/.style={->, line width=1.3pt, blue!55!black},
        learn/.style={->, line width=0.8pt, densely dashed, orange!75!black},
        gatec/.style={->, line width=1.1pt, green!45!black},
        tleup/.style={->, line width=0.7pt, black!55, dashed}
    ]
    % ── space segment (top right) ──
    \draw[black!35, dashed, line width=0.6pt]
        (11.2,5.75) .. controls (13.4,6.9) and (17.4,7.15) .. (19.6,6.15);
    \node[font=\tiny, text=black!50, rotate=14] at (12.15,6.35) {LEO pass};
    \node[blk, fill=blue!8] (sat) at (15.9,6.55) {LEO satellite\\LR-FHSS receiver};

    % ── ground segment (top left) ──
    \node[blk, fill=blue!8] (st) at (2.1,6.55) {Space-Track\\TLE updates};

    % ── IoT endpoint container ──
    \fill[green!5] (0.72,0.52) rectangle (13.18,4.42);
    \fill[gray!7]  (13.42,0.52) rectangle (18.08,4.42);
    \draw[black!60, line width=0.8pt, rounded corners=4pt] (0.6,0.4) rectangle (18.2,4.95);
    \node[font=\scriptsize\bfseries, anchor=west, text=black!70] at (0.85,4.68) {IoT endpoint};
    \draw[black!40, dashed, line width=0.5pt] (13.3,0.52) -- (13.3,4.42);
    \node[font=\tiny\itshape, text=green!35!black, anchor=east] at (13.1,0.78) {control plane};
    \node[font=\tiny\itshape, text=black!55, anchor=east]       at (18.0,0.78) {data plane};

    % ── control plane ──
    \node[blk] (stale) at (2.55,3.6) {Stale onboard\\TLE};
    \node[blk, fill=blue!10] (sgp4) at (5.95,3.6) {SGP4 physics\\baseline};
    \node[blk] (sel) at (9.55,3.6) {Selector /\\fail-safe};
    \node[blk] (hist) at (2.55,1.55) {Residual history /\\validation window $V$};
    \node[blk, draw=orange!75!black] (cand) at (6.15,1.55) {Learned residual\\candidate (optional)};
    \node[blk, draw=green!45!black, line width=1pt, fill=green!10] (gate) at (9.35,1.55)
        {\textbf{Evidence}\\\textbf{Gate}};
    \node[blk] (out) at (12.05,2.6) {TX timing guard\\hop-bin margin\\energy policy};

    % ── data plane ──
    \node[blk] (mcu)   at (14.55,2.6) {MCU};
    \node[blk] (modem) at (16.75,2.6) {LR1121\\modem};

    % ── flows ──
    \draw[tleup] (st.south) -- node[right, font=\tiny, text=black!55, pos=0.42]
        {infrequent TLE update} (stale.north);
    \draw[sig] (stale) -- (sgp4);
    \draw[phys] (sgp4) -- node[above, font=\tiny, text=blue!55!black] {default} (sel);
    \draw[learn] (stale.south) -- ++(0,-0.45) -| (cand.140);
    \draw[learn] (hist) -- (cand);
    \draw[learn] (cand) -- (gate);
    \draw[gatec] (gate.north) -- node[right, font=\tiny, text=green!40!black] {$G$} (sel.south);
    \draw[sig] (sel.east) -- ++(0.35,0) |- (out.170);
    \draw[sig] (out) -- (mcu);
    \draw[sig] (mcu) -- (modem);
    \draw[phys] (modem.north) -- node[sloped, above, font=\tiny]
        {LR-FHSS uplink burst} (sat.south);

    % ── scope footer ──
    \node[font=\tiny\itshape, text=gray!70, align=center] at (9.4,-0.05)
        {Software/model-derived control; conducted IQ evidence reported separately (Sec.~V).};
    \end{tikzpicture}}
    \caption{System view of evidence-gated endpoint control. The terminal uses
    stale orbital information to predict pass timing and Doppler, evaluates
    whether a learned residual is supported by a chronological validation window,
    and selects timing/frequency/energy margins for LR-FHSS uplink. The learned
    branch is optional and gated; the default deployment action is the SGP4
    physics baseline.}
    \label{fig:architecture}
\end{figure*}

\subsection{The Evidence Gate}
\label{sec:gate}
Let $V$ be a chronological \emph{validation} window, strictly later than the
training span and strictly earlier than the held-out test span, so that no future
TLE leaks into model selection. On $V$ we measure the mean absolute frequency
error of the physics baseline and of the learned corrector against the
model-derived reference,
\begin{align}
\mathrm{MAE}_{\mathrm{phys}}(V) &= \tfrac{1}{|V|}\textstyle\sum_{t\in V}\big|D_{\mathrm{ref}}(t)-f_{\mathrm{phys}}(t)\big|,\\
\mathrm{MAE}_{\mathrm{ml}}(V)   &= \tfrac{1}{|V|}\textstyle\sum_{t\in V}\big|D_{\mathrm{ref}}(t)-f_{\mathrm{ml}}(t)\big|.
\end{align}
The Evidence Gate (Fig.~\ref{fig:architecture}) enables learning only on a proven margin,
\begin{equation}
G = \mathbf{1}\!\left[\,\mathrm{MAE}_{\mathrm{ml}}(V) < \gamma\,\mathrm{MAE}_{\mathrm{phys}}(V)\,\right],
\qquad \gamma\in(0,1],
\label{eq:gate}
\end{equation}
and the deployed corrector defaults to physics otherwise,
\begin{equation}
\hat f(t) = G\,f_{\mathrm{ml}}(t) + (1-G)\,f_{\mathrm{phys}}(t).
\label{eq:deploy}
\end{equation}

By construction $\mathrm{MAE}_{\hat f}(V)\le \mathrm{MAE}_{\mathrm{phys}}(V)$
\emph{on the validation window only}---not a distribution-shift bound. The gate is
a binary test with two error modes: a \emph{false open} deploys a noisy learner
with nothing to correct, a \emph{missed open} keeps the physics baseline and
forgoes a smaller margin; $\gamma$ is the conservatism knob. We use
$\gamma=0.95$ and report sensitivity across $\gamma$ and validation-window size
(Sec.~\ref{sec:eval-sens}), making no net-improvement claim; the gate re-checks
the learned branch on each fresh window and reverts to physics whenever the
evidence lapses. MAE is used as the gate loss because it is stable on small
chronological windows; tail/outage proxies are reported after gating and do not
override the deploy/no-deploy decision.
\label{sec:stat}

\subsection{Control Proxies}
\label{sec:proxies}
The deployed error $e(t)=D_{\mathrm{ref}}(t)-\hat f(t)$ sets the frequency margin
to reserve and the chance a hop falls outside its bin. Three proxies are used
across all experiments: the \emph{guard-band proxy} $g = 2\,p_{99}(|e|)$; the
\emph{outage proxy} $\rho = \Pr[\,|e| > F_{\mathrm{tol}}\,]$ with a representative
sub-kHz bin tolerance $F_{\mathrm{tol}}=500$~Hz; and the dimensionless
\emph{energy/overhead proxy}
\begin{equation}
E_{\mathrm{proxy}} \;\propto\; \Big(1 + \alpha_g\,\tfrac{g}{B}\Big)\,(1 + \rho),
\label{eq:eproxy}
\end{equation}
where $B$ is the hopping bandwidth and $\alpha_g\!\ge\!0$ weights idle guard
against retransmission pressure. All three are control-layer proxies for terminal
margin sizing, not packet-error, bit-error, or link-budget measurements; the
500~Hz tolerance is a representative sub-kHz hop-bin/control proxy, not a
standard receiver threshold. Timing
couples in through the Doppler rate, which peaks near closest approach: a
residual timing offset $\Delta t$ costs frequency margin
\begin{equation}
\Delta f_D(t) \;\approx\; \dot f_D(t)\,\Delta t,
\label{eq:ratecoupling}
\end{equation}
motivating pass-aware allocation; the reported guard $g$ remains a scalar
pass-aggregated $p_{99}$ statistic.

For the endpoint-budget studies (Sec.~\ref{sec:eval-endpoint}) the same
philosophy yields a \emph{coverage-proxy chain}, stated here exactly as
implemented in the released scripts. Model the residual timing offset and
residual frequency error as zero-mean Gaussians,
$\delta_t\!\sim\!\mathcal{N}(0,\sigma_t^2)$ and
$f_{\mathrm{res}}\!\sim\!\mathcal{N}(0,\sigma_f^2)$. The terminal reserves an
uncertainty-sized timing guard
\begin{equation}
g_t = g_0 + k\,\sigma_t, \qquad k=3,
\label{eq:guard}
\end{equation}
and the two miss proxies are the Gaussian tail coverages
\begin{equation}
P_t = \Pr[|\delta_t|>g_t] = \operatorname{erfc}\!\Big(\tfrac{g_t}{\sigma_t\sqrt{2}}\Big),
\quad
P_f = \operatorname{erfc}\!\Big(\tfrac{F_{\mathrm{tol}}}{\sigma_f\sqrt{2}}\Big).
\label{eq:miss}
\end{equation}
Treating the two mechanisms as independent gives the joint success proxy
\begin{equation}
S = (1-P_t)(1-P_f),
\label{eq:joint}
\end{equation}
and each attempt is charged the receiver-on guard/listen energy plus the
transmit energy, so the energy per successful burst is
\begin{equation}
E_{\mathrm{att}} = I_{\mathrm{rx}}V\,(g_t + t_{\mathrm{rx}}) + P_{\mathrm{tx}}\,t_{\mathrm{tx}},
\qquad
E_{\mathrm{succ}} = E_{\mathrm{att}}/S.
\label{eq:esucc}
\end{equation}
Equation~\eqref{eq:guard} explains the Fig.~\ref{fig:proxies} shape: the
adaptive guard pins $P_t$ near the $k$-sigma floor
($\operatorname{erfc}(3/\sqrt{2})\!\approx\!0.3\%$) while $g_t$---and hence
$E_{\mathrm{att}}$---grows linearly with $\sigma_t$, so timing uncertainty is
paid in guard energy rather than misses. The timing-only sweep
(Fig.~\ref{fig:proxies}(a,b)) uses $S=1-P_t$; the ablation
(Fig.~\ref{fig:proxies}(c,d)) uses Eq.~\eqref{eq:joint}.

\subsection{Datasets and Splits}
\label{sec:data}
Real evaluation uses Space-Track TLE history for two BLACK~KITE LEO satellites:
BLACK~KITE-1 (NORAD~66741, 415 records, median inter-TLE gap $6.3$~h) and
BLACK~KITE-2 (NORAD~68474, 184 records, median gap $6.4$~h). For each reference
(newer) TLE, an older TLE inside a target staleness band is paired with it,
propagated open-loop, and compared to the reference at 24 UTC samples over one
orbital period; outlier or bad-fit pairs are rejected. Splits are chronological by
reference epoch (60/20/20 train/validation/test), and the validation window uses
only completed TLE-refresh intervals whose newer TLE arrived before the next
deployment decision, so the gate is causal. The synthetic stress study
(Sec.~\ref{sec:eval-stress}) is a controlled simulation, no Space-Track data.

At each control epoch the terminal re-evaluates Eqs.~\eqref{eq:gate}--\eqref{eq:deploy}
on $V$ and re-sizes $g=2\,p_{99}(|e|)$---no receiver feedback, recomputed
whenever a fresh TLE arrives.

% ── Evaluation ────────────────────────────────────────────────────────────────
\section{Evaluation}
\label{sec:eval}

\subsection{Experimental Setup}
\emph{Carrier convention.} Software Doppler and control metrics are reported at
an 868~MHz carrier (python-SGP4, ground station of Sec.~\ref{sec:baseline}),
matching common LR-FHSS D2S evaluation settings; the controller is
frequency-parametric (Eq.~\eqref{eq:doppler}) and residuals scale linearly with
$f_c$. The conducted-IQ run of Sec.~\ref{sec:hw} uses 923.2~MHz for the local
AS923 laboratory setup and is reported only as measurement-path evidence, not
as measured Doppler truth for Table~\ref{tab:bk}. Learned
candidates are ridge, random forest, gradient boosting, and a small MLP, selected
on the chronological validation segment with the gate evaluated once on the
held-out test segment; a window counts as supported only if the selected model
beats the zero-residual baseline on held-out MAE by a clear margin and also
improves RMSE. All results in Secs.~\ref{sec:eval-real}--\ref{sec:eval-endpoint}
are software-only and model-derived; no hardware, RF, UART, replay, or measured
Doppler is involved.

\subsection{Real BLACK~KITE Negative Result}
\label{sec:eval-real}
Table~\ref{tab:bk} and Fig.~\ref{fig:gateev}(a,b) report the held-out test MAE of
the validation-selected learned model against the zero-residual stale-TLE
baseline: the learned residual is worse at \emph{every} tested staleness, both
target-specific (BK1, 8--168~h) and cross-satellite (BK1$\rightarrow$BK2), so the
gate of Eq.~\eqref{eq:gate} closes in all cases. Even at 168~h the BK1 baseline
MAE is only 26.9~Hz---small against $F_{\mathrm{tol}}=500$~Hz---so the learner
must improve an already small residual, where generalization noise loses the
held-out comparison. This is the paper's central real-data finding.

The result is consistent with how the data arises: successive Space-Track TLEs
reflect orbit-determination updates over different observation arcs, so the
inter-TLE residual mixes fit artifacts, drag mismatch, tracking noise, and
maneuver-like events---near zero-mean from the terminal-feature perspective, so
a regressor fits validation noise rather than a stable correction; richer
features or longer staleness could still expose learnable structure, precisely
the case the gate admits. Both $D_{\mathrm{ref}}$ and $f_{\mathrm{phys}}$ are
SGP4 propagations: a model-to-model inter-TLE residual, not a measured
RF-channel residual.

\begin{table}[tb]
\centering
\footnotesize
\caption{Real BLACK~KITE negative result: held-out test MAE [Hz] at the 868~MHz
software carrier, learned residual vs.\ zero-residual stale-TLE baseline.
Values are model-derived from TLE/SGP4 and scale linearly with carrier
frequency; no measured Doppler is used
(\texttt{reference\_is\_measured\_truth=false}). The gate closes in every row
because the learned residual is worse than the physics baseline.}
\label{tab:bk}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{llccr c}
\toprule
Setting & Stale & Base & Learned & Degr.\% & Gate \\
\midrule
BK1 target & 8\,h   & 0.2430 & 0.3501 & $44.1$ & closed \\
BK1 target & 24\,h  & 0.8161 & 0.9109 & $11.6$ & closed \\
BK1 target & 48\,h  & 1.9433 & 2.8608 & $47.2$ & closed \\
BK1 target & 72\,h  & 4.8947 & 5.9092 & $20.7$ & closed \\
BK1 target & 96\,h  & 10.1153& 11.7663& $16.3$ & closed \\
BK1 target & 168\,h & 26.9243& 45.2629& $68.1$ & closed \\
\midrule
BK1$\to$BK2 & 8\,h  & 0.1877 & 0.3261 & $73.7$ & closed \\
BK1$\to$BK2 & 24\,h & 0.4969 & 1.8639 & $275.1$& closed \\
BK1$\to$BK2 & 48\,h & 2.4092 & 2.8458 & $18.1$ & closed \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure*}[!t]
    \centering
    \includegraphics[width=0.92\textwidth]{figures_final/fig_gate_evidence.pdf}
    \caption{Evidence-gate behaviour. (a,b)~\emph{Real} BLACK~KITE result
    (model-derived, no measured Doppler): learned MAE above baseline, degradation
    positive everywhere, gate closes. (c,d)~\emph{Synthetic sanity check}: gate
    opens only in the systematic regime; $\gamma$ matters only at the margin.}
    \label{fig:gateev}
\end{figure*}

\subsection{Synthetic Stress: When the Gate Opens}
\label{sec:eval-stress}
A controlled synthetic simulation (no Space-Track data) generates the residual as
$r = A\,s(x) + \mathcal{N}(0,\sigma^2)$ with a fixed nonlinear systematic
$s(\cdot)$ and per-regime $(A,\sigma)$ bracketing gate behaviour; outcomes are
summarised in Table~\ref{tab:gate} and Fig.~\ref{fig:gateev}(c,d). The real
BLACK~KITE experiment is thus a \emph{negative control} for unsafe learning:
when the residual lacks stable learnable structure, the gate correctly retains
SGP4. The synthetic systematic regime verifies the complementary case: when a
dominant residual structure exists, the same rule opens and reduces the
residual, guard, and outage proxies (Table~\ref{tab:gate}; overhead proxies, not
measured power savings). The gate is therefore a deploy/no-deploy test, not an
always-learn mechanism.

\begin{table}[tb]
\centering
\footnotesize
\caption{Gate behaviour across regimes: closed on the real data (negative
control), open only under evidenced systematic residual (synthetic).}
\label{tab:gate}
\setlength{\tabcolsep}{3pt}
\begin{tabular}{lll}
\toprule
Regime & Held-out evidence ($\gamma{=}0.95$) & Gate \\
\midrule
BLACK~KITE (real) & learned worse, 8--168~h & closed \\
Synth.\ noise-dom. & no learnable structure & closed \\
Synth.\ marginal & $\sim$5\% gain $<$ margin & closed \\
Synth.\ systematic & MAE 1859.3$\to$99.8~Hz & \textbf{open} \\
 & guard 14.1~kHz$\to$950~Hz & \\
 & outage 0.814$\to$0.008 & \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Sensitivity}
\label{sec:eval-sens}
Fig.~\ref{fig:gateev}(c,d): both decisive regimes are $\gamma$-insensitive
across $[0.90,1.00]$; only the marginal regime flips between $0.95$ and $0.99$.
Decisions hold across validation sizes $n\in\{100,300,1000,3000\}$ except
$n\!\approx\!100$ near the threshold. The real BK1/BK2 segments sit deep in the
noise-dominated regime, so the real-data decision (closed) is unambiguous; we
adopt $\gamma=0.95$.


\begin{figure*}[!t]
    \centering
    \includegraphics[width=0.92\textwidth]{figures_final/fig_endpoint_proxies.pdf}
    \caption{Endpoint-control proxies (software-only coverage proxies, not
    measured packet outcomes). (a,b)~Timing sensitivity under the adaptive
    $3\sigma$ guard; markers: SGP4 open-loop staleness (TLE 24/168~h).
    (c,d)~Control ablation (log axes); controlled \mbox{*gate}-open
    \emph{synthetic} regime
    (real gate closes, Sec.~\ref{sec:eval-real}).}
    \label{fig:proxies}
\end{figure*}
\begin{table}[tb]
\centering
\footnotesize
\caption{Proxy model and evaluation parameters
(Eqs.~\eqref{eq:guard}--\eqref{eq:esucc}; values as implemented in the released
scripts).}
\label{tab:params}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{ll}
\toprule
Parameter / proxy & Value / definition \\
\midrule
Pass duration $T_{\mathrm{pass}}$ & 240~s \\
Timing guard & $g_0=30$~ms; $g_t = g_0 + 3\sigma_t$ \\
TX burst & 14~dBm, $t_{\mathrm{tx}}=200$~ms \\
RX listen & $t_{\mathrm{rx}}=50$~ms, 12~mA @ 3.3~V \\
Hop-bin tolerance & $F_{\mathrm{tol}} = 500$~Hz proxy \\
Timing / freq.\ miss & $P_t,\,P_f$, Eq.~\eqref{eq:miss} \\
Success; energy/success & $S$, Eq.~\eqref{eq:joint}; $E_{\mathrm{att}}/S$, Eq.~\eqref{eq:esucc} \\
Guard overhead & $g_t / T_{\mathrm{pass}}$ \\
TLE age $\to \sigma_t$ & $\sim$1.5~km/day $\div$ 7.67~km/s \\
Gate threshold; window & $\gamma=0.95$; chronological $V$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Endpoint-Control Proxies}
\label{sec:eval-endpoint}
Two control-proxy studies instantiate the coverage-proxy chain of
Eqs.~\eqref{eq:guard}--\eqref{eq:esucc} with the parameters of
Table~\ref{tab:params}---coverage proxies rather than measured packet outcomes.

\emph{Timing uncertainty is paid as guard energy}
(Fig.~\ref{fig:proxies}(a,b)): sweeping $\sigma_t$ from 16~ms to 2~s under the
$3\sigma$ guard keeps the miss proxy near the $3\sigma$ floor ($\approx$0.3\%)
but inflates guard overhead from 0.03\% to $\sim$2.5\% of the pass and energy per
successful burst from $\sim$10~mJ to $\sim$246~mJ; open-loop SGP4 staleness
($\sim$1.5~km/day along-track over orbital speed) places TLE age 8~h at
$\sigma_t\!\approx\!0.07$~s and 168~h at $\sigma_t\!\approx\!1.4$~s on this curve.

\emph{Joint timing/frequency control dominates either dimension alone}
(Fig.~\ref{fig:proxies}(c,d)): joint success rises 0.03\% (no control) $\to$
2.0\% (timing only; the $\sim$2.5~kHz SGP4 residual misses the 500~Hz bin 84\% of
the time) $\to$ 15.8\% (SGP4 timing$+$frequency) $\to$ 90.4\% with the learned
predictor's calibrated residuals ($\sigma_t{=}16$~ms, $\sigma_f{=}300$~Hz), and
energy per successful burst falls from 1.18~J to 11.2~mJ. The last step is the
controlled gate-open \emph{synthetic} regime; on the real data the gate stays closed and the
controller equals the baseline (Sec.~\ref{sec:eval-real}).

\emph{The predictor footprint is bounded, inference-only, and MCU-class when
Flash suffices}: 333\,720 parameters, 332\,288 MACs ($\approx$0.66~MFLOP) per
inference, $\approx$326~KB int8 Flash, $<$1~KB working RAM (no claim for
M0-class nodes without on-device validation; Flash-feasible on mid-range
MCU-class devices, not ultra-minimal nodes). Training is offline; the endpoint
runs one inference per pass, estimated at $\sim$12.5~ms / $\sim$1.07~mJ on a
Cortex-M4F-class core---$\approx$6.4\% of one burst's transmit draw,
$\approx$0.3\% per 20-burst pass; an estimate, not an on-device measurement.

% ── Preliminary Conducted-IQ Evidence ─────────────────────────────────────────
\section{Preliminary Conducted-IQ Evidence}


\label{sec:hw}

Table~\ref{tab:hw} summarizes the conducted measurement-path sanity check.
NUCLEO-L476RG $+$ LR1121 board was reflashed to deterministic 923.2~MHz /
$-17$~dBm firmware and cabled through 50~dB attenuation into USRP~B210 RX2\,A
with no antenna. The reflash changed the 923~MHz capture from not visible under
stock 868~MHz firmware to a repeatable $41.25\pm0.36$~dB TX-ON/TX-OFF
separation, reproduced at 2~MS/s and after DC/LO artifact masking.
These observations verify the cabled IQ measurement path only; this 923.2~MHz
local AS923 run is separate from the 868~MHz software metrics and does not
exercise Doppler correction, evidence gating, packet decode, or over-the-air
behavior.

\begin{table}[!htbp]
\centering
\small
\caption{Conducted-IQ sanity check summary.}
\label{tab:hw}
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.08}
\begin{tabular}{@{}p{0.31\columnwidth}p{0.62\columnwidth}@{}}
\toprule
Check & Summary \\
\midrule
Firmware & LR1121 deterministic TX, 923.2~MHz / $-17$~dBm \\
Path & LR1121 / NUCLEO-L476RG $\to$ 50~dB attenuated coax $\to$ USRP~B210 RX2\,A \\
Serial verification & \texttt{TX\_START} $\to$ repeated LR-FHSS bursts $\to$ \texttt{TX\_DONE} \\
Before/after control & stock 868~MHz not visible in 923~MHz span ($\sim$0.5~dB); 923.2~MHz visible \\
Repeatability & $41.25\pm0.36$~dB TX-ON/TX-OFF over four 4~MS/s runs \\
Streaming sanity & 43.76~dB at 2~MS/s \\
Artifact handling & DC/LO masked; TX-ON remains visible near hop-grid proxy bin \\
Boundary & conducted IQ only; no packet decode, PER/PDR/CRC, gateway ACK, OTA, or link validation \\
\bottomrule
\end{tabular}
\end{table}

% ── Conclusion and Limitations ────────────────────────────────────────────────
\section{Conclusion and Limitations}
\label{sec:conclusion}

We recast PHY-layer Doppler compensation as timing- and
frequency-uncertainty-aware endpoint control for LR-FHSS D2S IoT under stale
orbital information: open-loop SGP4 is the default, and a learned residual is
deployed only when a chronological held-out window beats the baseline by a
margin. On real BLACK~KITE TLE history the learned correction never beats the
baseline, so the gate closes and the terminal keeps the conservative fallback;
synthetically the gate opens only under dominant systematic drift, where the
proxies show joint timing--frequency prediction converts a high-miss,
energy-hungry proxy endpoint into a lower-miss synthetic case at a mid-range
MCU-class, inference-only footprint estimate. The Evidence Gate is thus a
trust/deploy decision for learning, not a guaranteed-improvement claim. The
design rule: never deploy a learned residual
unconditionally; deploy it only after a validation window clears $\gamma$; keep
the gate auditable and reversible (log $G$, $\gamma$, the validation MAE pair,
$|V|$, TLE age); and let the selected prediction and its uncertainty size the
three endpoint margins---TX timing guard, hop-bin margin, energy policy---via
Eqs.~\eqref{eq:guard}--\eqref{eq:esucc}.

\label{sec:limits}
All control results are software-only and model-derived
(\texttt{reference\_is\_measured\_truth=false}): no measured Doppler truth,
live-satellite contact, standards-compliant LR-FHSS decoding, PER/BER/CRC/PDR,
or gateway acknowledgement. The negative result holds for two
BLACK~KITE satellites and our feature set, not universally; the positive learning
result is synthetic only; the gate gives a validation-window property, not a
distribution-shift bound; the proxies of Sec.~\ref{sec:eval-endpoint} are control
proxies, not link-budget measurements; and the MCU latency/energy figures are
estimates. The hardware evidence of Sec.~\ref{sec:hw} is conducted IQ-level
measurement-path evidence, not link-layer validation: no packets were decoded,
and no over-the-air or satellite path was exercised. Packet-level
conducted PER/PDR against a reference receiver and authorized over-the-air
measurement are future work, in that order.

% ── References ───────────────────────────────────────────────────────────────
\bibliographystyle{IEEEtran}
\bibliography{refs}

\end{document}
